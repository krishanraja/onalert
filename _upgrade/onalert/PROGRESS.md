# OnAlert rebuild — progress + resume state

Date: 2026-05-30
Branch: `upgrade/onalert/phase-2-hardening` (off `main` @ 2bac868). main is UNTOUCHED.
Local toolchain: Node 22 LTS via fnm (run `eval "$(fnm env --shell bash)"; fnm use 22` in every shell). Build + lint green.
Decisions locked: scope = Everything incl. AI; rendering = build-time prerender of public routes (vite-react-ssg); attribution = central OS warehouse `gojpffsrxybbpbdzzrvs`, build emit now; push to main at the END after preview-deploy verification; OS-handoff summary -> `C:\Users\krish\Downloads\app OS summaries\Onalert.md`.

## DONE + verified + deployed (committed on the branch)
1. **Phase 0 verified recon** — `_upgrade/onalert/PHASE-0.md`.
2. **Security: booking_clicks cross-tenant leak FIXED** — migration `018_fix_booking_clicks_cross_tenant_read.sql` applied to prod via Management API + verified. Commit bb82fef.
3. **Green baseline** — fnm + Node 22; root cause was a corrupted csstype, not Node 25.
4. **Single-source pricing + product truth** (commit 8a94895) — canonical `src/data/pricing.json` + `src/data/product-truth.base.json`; generator `scripts/gen-product-truth.mjs` emits `supabase/functions/_shared/pricing.ts` (committed), `public/.well-known/product-truth.json` + `public/llms.txt` (gitignored, built on prebuild/predev). `create-checkout` + `stripe-webhook` import the shared cents (no more 3-file fork). DEPLOYED + smoke-tested.
5. **Attribution spine** (commit 17c77ed) — first-paint capture (index.html) -> `src/lib/attribution.ts` -> signup user_metadata (AuthPage) -> Stripe customer+session metadata (create-checkout) -> server-side purchased/refunded (stripe-webhook) -> `track-lifecycle` edge fn forwards to OS warehouse via `_shared/lifecycle.ts`. landed/signed_up/activated emits wired. DEPLOYED + smoke-tested. Dormant until `VITE_ATTRIBUTION_ENABLED=true` + `ATTRIBUTION_INGEST_URL`/`ATTRIBUTION_INGEST_SECRET` set (no-op verified).
6. **CBP Book deep-link fix** (commit 91b18ac) — `buildBookUrl` (both copies) now uses real service codes (up/nh/sh) + verified URL template; per-location deep-link not supported by CBP (don't over-promise). Redeployed send-alert, send-digest-alert, process-rechecks, poll-appointments.

7. **Public surface prerendered (#6)** — vite-react-ssg, 55 static pages, per-page meta+JSON-LD, generated sitemap, /app loading-shell hydration, vercel.json rewrites. Commits 26d90aa + 4764c6b. Build/lint/preview verified. (Deployable as-is — pure frontend + Vercel config.)
8. **Push works end-to-end in code + crons + DST (#7)** — custom SW src/sw.ts (push+notificationclick), send-push rewritten (RFC 8292 VAPID + RFC 8291 aes128gcm, Web Crypto, unit-tested 2/2), wired into send-alert; setup-cron-jobs.sql schedules process-rechecks + predict-slots (were dead); easternEndOfDay DST-correct; fixed 2 PostgREST .catch type errors. Commits 3982842, e9e67da, 6c86581, d7af765. DEPLOY-GATED (deploy fns + VAPID secrets + run cron SQL).
9. **Perf code-split + latent-crash fix + docs (#9/#12)** — lazy-split 8 authed pages (626→457KB raw); fixed AlertDetailPage missing `cn` import (would crash; masked by stale tsbuildinfo); FEATURES.md + product-truth seoPages→live. Commits 9a201a7, 2e1f*, 9c8c8a3. Cache-cleared tsc across app = 0 errors.

SESSION 2026-05-30 (post-/clear) SUMMARY: #6 + #7 + #12 done in code; #9 partial (code-split + crash fix done; design polish deferred). OS handoff written to C:\Users\krish\Downloads\app OS summaries\Onalert.md. NOT merged to main. Remaining: #8 AI (not started), #9 design polish (needs browser), #7 NEXUS/SENTRI data task, then #10 Vercel preview + merge.

Edge functions on the branch with new/changed code (NOT all deployed yet): create-checkout, stripe-webhook, track-lifecycle, send-alert (push wiring), send-digest-alert, process-rechecks, poll-appointments (DST + catch fix), send-push (VAPID rewrite). DEPLOY the last three + send-push for #7 to go live.

## REMAINING (not started or in progress)
- **#6 Public surface** (CORE DONE — commits 26d90aa + 4764c6b on branch): vite-react-ssg@0.9.0 migration LANDED + build/lint green + preview-server-verified. DONE: App.tsx -> `routes: RouteRecord[]` w/ pathless RootLayout (StrictMode+ErrorBoundary, NO HelmetProvider — framework supplies it); main.tsx -> `ViteReactSSG({routes}, ({isClient})=>...)` w/ landed-attribution behind isClient; all 10 Helmet pages + AuthPage now use `<Head>`; JSON-LD SoftwareApplication moved to LandingPage only (out of index.html); per-location GovernmentOffice+BreadcrumbList JSON-LD; vite.config `ssgOptions.includedRoutes` (NB: discovered paths arrive WITHOUT leading slash except '/', so normalize before matching) + `getStaticPaths` over TOP_LOCATIONS; build = `vite-react-ssg build`. **55 static HTML pages emitted** (6 public + 45 locations + /app shell + /404). GOTCHA RESOLVED EMPIRICALLY (read compiled index.mjs): prod ALWAYS hydrateRoots (CSR only in dev when no data-server-rendered attr) — so /app is prerendered as the AppLayout LoadingSpinner shell (authed===null short-circuits before AlertsProvider/Supabase), client first-render = same spinner = clean hydrate; vercel.json rewrites `/app/:path* -> /app.html`, cleanUrls:true, /app + /app/* noindex. sitemap.xml now generated in gen-product-truth.mjs from src/lib/locations.ts (51 URLs, exact match to prerendered set, kills the 48 soft-404s + 43 missing; zero private routes leak). WaitTimes swallowed-error -> explicit error state. STILL TODO on #6 (lower priority, not blocking ship): per-location loader baking live slot stats from public-wait-times (currently client-fetched via useLocationIntelligence — works, just not prerendered); per-page @vercel/og images; "CBP Appointment Weather" public page + /api/weather feed; correct DECISIONS_LOG D004. NOTE: real in-browser hydration test deferred to #10 Vercel preview (Playwright MCP not connected this session); preview server confirmed served HTML + bundle 200 + clean URLs + shell.
- **#7 Fix-the-magic** (CORE DONE — commits 3982842, e9e67da, 6c86581, d7af765): push notifications now work END TO END in code. DONE: (a) custom SW src/sw.ts via injectManifest with `push` + `notificationclick` deep-link handlers (Workbox precache preserved); (b) send-push rewritten as standards-compliant Web Push — RFC 8292 VAPID ES256 JWT + RFC 8291 aes128gcm (ECDH P-256 -> HKDF -> AES-128-GCM) using only Web Crypto, with _shared/webpush.ts + webpush.test.ts (2/2 pass: payload round-trip + JWT verify); (c) WIRED send-push into send-alert (it was NEVER called before — push was dead) parallel to email, deep-linking to the CBP book URL, recording `push` channel only on real delivery; covers immediate + delayed; (d) setup-cron-jobs.sql now schedules process-rechecks (*/5) + predict-slots (daily 08:00 UTC) — both were unscheduled/dead — and is idempotent; (e) EDT/DST bug fixed: easternEndOfDay uses Intl-derived America/New_York offset, DST-exact (verified summer/winter); also fixed 2 PostgREST .catch()-on-builder type errors so `deno check` poll-appointments is clean. DEPLOY-GATED (Krish): `supabase functions deploy send-push send-alert poll-appointments`; run setup-cron-jobs.sql with rotated key + CRON_SECRET; set VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY/VAPID_SUBJECT (npx web-push generate-vapid-keys; PUBLIC must == client VITE_VAPID_PUBLIC_KEY). STILL TODO (larger, deferred): Live Slot screen (frontend); NEXUS/SENTRI coverage gap — `fetchLocation` hardcodes CBP `serviceId=TP` and TOP_LOCATIONS is 42/45 GE-only; needs real NEXUS(border)/SENTRI(southern-border) enrollment-center IDs sourced + verified against the live CBP schedulerapi (data task, do NOT guess IDs).
- **#8 AI layer** (FOUNDATION DONE — commit 4cf08d3): shared `supabase/functions/_shared/anthropic.ts` built + unit-tested (anthropic.test.ts, 7/7, injected fetch no network). Current Messages API (POST /v1/messages, anthropic-version 2023-06-01), Haiku 4.5 default. CONTRACT: `generateText()` returns `string|null`, NEVER throws — every failure (no key, AbortController timeout, non-2xx, network, malformed/refusal/empty) degrades to null so callers keep their deterministic heuristic and AI stays OFF the <30s alert path. `extractText()` pure-parse exported + tested. REMAINING (all build on this; DEPLOY-GATED — need live ANTHROPIC_API_KEY + edge deploy to verify; several touch hot path so wire as opt-in enrichment with heuristic fallback): Haiku narration replacing predict-slots/poll `formatNarrativeFromHistory` heuristic; reasoning ranker (grab-it score + rationale) OFF the <30s path; NL/voice monitor setup (Sonnet tool-call, resolve_locations bound to catalog); CBP-letter vision intake; concierge guidance; re-engagement; anomaly narration. ANTHROPIC_API_KEY already set in prod edge secrets. NOTE: the Read tool glitched on poll-appointments/index.ts this session (rendered phantom 52k lines + fake content) — file is verified intact at 876 lines, deno-clean; re-Read fresh before editing it.
- **#9 Design pass**: system spine + every-pixel + magic-moment micro-interactions; value-first anonymous onboarding; collapse duplicate alert-detail renderers; code-split the 576KB main chunk.
- **#12 Docs**: refresh docs/ (SALES_PLAYBOOK, VALUE_PROP, ICP, FEATURES capability status, OUTCOMES, STRATEGY, AGENT_BRIEFING) so an agent can autonomously sell+market; correct D004 + push status. Must agree with product-truth.json + llms.txt.
- **#10 Verify + ship**: Vercel preview deploy; mobile walk 390/375/430; perf+a11y; view-source proves real HTML; structured-data validate; test conversion end-to-end; MERGE branch to main + push; write OS-handoff to `C:\Users\krish\Downloads\app OS summaries\Onalert.md`.

## NEEDS KRISH (does not block build)
- **Service-role key rotation** (#11): leaked JWT is LIVE; rotation regenerates the anon key -> must update Vercel `VITE_SUPABASE_ANON_KEY` + redeploy. Runbook in PHASE-0 chat.
- **Stripe webhook events** (#13): add `charge.refunded` + `charge.dispute.created` to the live OnAlert webhook endpoint (downgrade path code-ready but Stripe not sending those events). Auto-mode blocked the automated change.
- **Warehouse creds**: `ATTRIBUTION_INGEST_URL` + `ATTRIBUTION_INGEST_SECRET` (+ flip `VITE_ATTRIBUTION_ENABLED=true`) to make lifecycle emission live; the OS repo must build `ingest-attribution` + the `attribution` schema.

## KEY FACTS
- Prod footprint: 4 profiles (3 free, 1 manual pro), ZERO completed payments ever (clean slate).
- Deploy edge fns: `SUPABASE_ACCESS_TOKEN=sbp_... supabase functions deploy <fn> --project-ref zcreubinittdqyoxxwtp` (Docker not needed; `db push` is broken — Management API for migrations).
- Stripe (working key ends `...rDgIn00ws1tJoCg`; the ACCESS-block `...aANz` key is DEAD).
